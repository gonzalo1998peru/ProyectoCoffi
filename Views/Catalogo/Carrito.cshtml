<!DOCTYPE html>
<html>
<head>
    <title>Carrito de Compras</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #d5b097;
            padding: 20px;
        }
        .titulo {
            text-align: center;
            font-size: 2em;
            margin-top: 20px;
            margin-bottom: 20px;
            font-weight: bold;
        }
        .carritoContainer {
            display: flex;
            flex-wrap: wrap;
        }
        .productoEnCarrito {
            border: 1px solid #ccc;
            padding: 10px;
            margin: 10px;
            width: 47%; /* Ajusta el ancho para mostrar 2 productos en una fila */
            text-align: center;
        }
        .imagenCafeCarrito {
            max-width: 80px;
            max-height: 80px;
            margin-bottom: 10px;
        }
        .nombreProductoCarrito {
            margin-bottom: 5px;
        }
        .precioProductoCarrito {
            margin-bottom: 5px;
        }
        .totalCarrito {
            text-align: right;
            font-size: 1.5em;
            margin-top: 20px;
        }
        .botonPagar {
            padding: 10px;
            background-color: #4CAF50;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 1em;
        }
        .botonVaciarCarrito {
            padding: 10px;
            background-color: #ff6b6b;
            color: white;
            border: none;
            cursor: pointer;
            font-size: 1em;
        }
    </style>
</head>
<body>
    <div class="titulo">CARRITO DE COMPRAS</div>
    <div class="carritoContainer">
        <!-- Aquí se mostrarán los productos en el carrito dinámicamente -->
    </div>
    <div class="totalCarrito">
        Total: <span id="total">S/0</span>
    </div>
    <button class="botonPagar">Pagar</button>
    <button class="botonVaciarCarrito">Vaciar Carrito</button>

    <script>
        // JavaScript para manejar el carrito se añadirá aquí
    </script>
</body>
</html>
